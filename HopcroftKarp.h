#ifndef HOPCROFTKARP_H_
#define HOPCROFTKARP_H_

#include <stdlib.h>
#include <vector>
#include <queue>
#include <climits>
#include <list>
#include <cassert>

bool  	bfsHK();
bool 	dfsHK(int);
int 	HopcroftKarp(int, std::vector<std::vector<int>>);

#endif